# Polymarket CLOB credentials — copy to poly_clob.txt and fill in.
# NEVER commit poly_clob.txt. Deposit USDC/pUSD to FUNDER on Polygon.

# Export private key: Polymarket Settings → Account → Start Export
#   (opens https://reveal.magic.link/polymarket — log in with same email/Google)
PRIVATE_KEY=0xYOUR_MAGIC_EOA_PRIVATE_KEY

# Proxy / deposit wallet that holds funds (NOT the EOA/signer from the key).
# Portfolio data-api uses this as ?user=0x... — also fund USDC here on Polygon.
FUNDER=0xYOUR_PROXY_WALLET_ADDRESS

# Email/Magic login → usually 1; newer POLY_1271 Magic → 3 | browser wallet proxy → 2 | EOA → 0
SIGNATURE_TYPE=3

# Optional — leave blank to auto-derive on first connect()
API_KEY=
API_SECRET=
API_PASSPHRASE=

# Execution mode: paper | dry_run | live  (keep paper until gate passes)
MODE=paper

HOST=https://clob.polymarket.com
CHAIN_ID=137
